# per-developer state
/.fo/
/.direnv/
/result
/result-*

# node
node_modules/

# Phase 3 will GENERATE platform/idm/script/ from platform/typescript/, at
# which point this becomes `/platform/idm/script/`. Until then the scripts
# there are hand-authored sources and must be tracked.
