{
  "//": "Compiles only the engine-usage fixture, on the runtime program's exact lib. tests/engine-lib.test.mjs points the usage check at this to prove the check can still fail. Managed file - regenerated by `fo upgrade`.",
  "extends": "../../tsconfig.json",
  "include": ["uses-absent-builtins.ts"]
}
