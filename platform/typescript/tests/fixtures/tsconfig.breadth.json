{
  "//": "Compiles only the breadth fixture, on the runtime program's exact lib. tests/engine-lib.test.mjs asserts both what the usage check finds in it and what it does not. Managed file - regenerated by `fo upgrade`.",
  "extends": "../../tsconfig.json",
  "include": ["reaches-one-absent.ts"]
}
