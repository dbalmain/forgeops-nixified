{
  "//": "Compiles only the native-subclass fixture, on the runtime program's exact lib. tests/engine-lib.test.mjs points the gate at this to prove it can still fail. Managed file - regenerated by `fo upgrade`.",
  "extends": "../../tsconfig.json",
  "include": ["subclasses-natives.ts"]
}
