{
  "//": "The PingIDM endpoint program. `lib` is pinned to what the script engine actually provides - no Proxy, no Reflect, no Object.hasOwn, no Array#flat - because `@babel/preset-env` runs with `useBuiltIns: false` and polyfills nothing, so widening it makes runtime-impossible code type-check. The pin is still wider than the engine: 232 of the 728 declarations it carries are absent on PingIDM (the typed arrays, RegExp#flags, ...) and cannot be removed, so `fo build` fails on any USE of one it can resolve, before it emits - tools/engine-usage.mjs, which states exactly which forms those are and which are outside the proof. Every entry was verified against a running stack; the original spike is spike/ENGINE-SURFACE.md. Members of a lib entry that is only a PARTIAL match live in framework/engine-lib.d.ts. The PingAM scripts are a SEPARATE program (tsconfig.am.json) because the two runtimes declare colliding globals (both have a `logger`, and they are not the same shape). Managed file — regenerated by `fo upgrade`.",
  "compilerOptions": {
    "lib": [
      "ES5",
      "ES2015.Core",
      "ES2015.Collection",
      "ES2015.Iterable",
      "ES2015.Symbol",
      "ES2015.Symbol.WellKnown",
      "ES2015.Promise",
      "ES2016.Array.Include",
      "ES2017.String",
      "ES2018.Promise",
      "ES2019.Object",
      "ES2019.String"
    ],
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "erasableSyntaxOnly": true,
    "noEmit": true,
    "types": [],
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noPropertyAccessFromIndexSignature": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": false
  },
  "include": [
    "framework/**/*",
    "src/endpoints/**/*",
    "src/generated/**/*",
    "src/tasks/**/*",
    "src/shared/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "openapi",
    "framework/am.ts",
    "framework/am-globals.d.ts"
  ]
}
