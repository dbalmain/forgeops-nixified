{
  "//": "The test program. Separate from tsconfig.json because the tests run under node \u2014 they need `node:test` and a modern lib, while the endpoint program is deliberately pinned to the narrow PingIDM runtime lib. It also excludes the PingAM sources, whose globals collide with IDM's. Managed file \u2014 regenerated by `fo upgrade`.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": [
      "ES2022"
    ],
    "types": [
      "node"
    ]
  },
  "include": [
    "framework/**/*",
    "src/endpoints/**/*",
    "src/generated/**/*",
    "src/tasks/**/*",
    "src/shared/**/*",
    "tests/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "openapi",
    "framework/am.ts",
    "framework/am-globals.d.ts",
    "src/scripts/**/*",
    "tests/am-typing.ts"
  ]
}
